ext {
    android = [
            buildToolsVersion: "24.0.0",
            minSdkVersion    : 14,
            targetSdkVersion : 24,
            compileSdkVersion: 24,
            versionCode      : 2,
            versionName      : "1.0.1",
    ]

    dependencies = [
            googleSupportVersion: "24.2.1"
    ]
}