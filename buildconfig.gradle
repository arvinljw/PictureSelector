ext {
    android = [
            buildToolsVersion: "24.0.0",
            minSdkVersion    : 14,
            targetSdkVersion : 24,
            compileSdkVersion: 24,
            versionCode      : 1,
            versionName      : "1.0.0",

    ]

    dependencies = [
            googleSupportVersion: "24.2.1"
    ]
}